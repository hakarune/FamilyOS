# FamilyOS locked DNS - see Project_Vault/Internet Toggle with DNS
# Lockdown.md. This file is overwritten and re-locked every boot by
# overlays/etc/init.d/familyos-dns-lock - editing this copy directly
# has no lasting effect on a built image; it exists here only to seed
# the file's content before that script's own heredoc becomes the
# source of truth. Only the first 3 nameserver lines are honored by
# glibc's stub resolver (MAXNS=3) - the 4th is kept for documentation
# fidelity with the source doc's full list and is harmless if ignored.
nameserver 193.110.81.1
nameserver 185.253.5.1
nameserver 208.67.222.123
nameserver 208.67.220.123
