#!/bin/bash
# Cuts or restores external internet access while preserving the LAN.
#
# STATUS: stub. Real iptables logic is scheduled for Phase 3 - see
# Project_Vault/Internet Toggle with DNS Lockdown.md for the exact
# rules this will implement. This stub only wires up the auth/privilege
# contract (see parental-tools/README.md) so the CLI and GUI have a
# real, safe target to call in Phase 1.
set -euo pipefail
DIR="$(dirname "$(readlink -f "$0")")"
# shellcheck source=lib/require-parent-auth.sh
source "$DIR/lib/require-parent-auth.sh"

require_parent_auth

echo "familyos-net-toggle: not yet implemented (Phase 3)." >&2
exit 2
