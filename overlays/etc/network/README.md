# overlays/etc/network

Reserved for network configuration injected into the live ISO root
(interface config, `net-toggle` hooks per `Github Project Structure.md`).
The actual `net-toggle` logic lives in `../../../parental-tools/` and is
scheduled for Phase 3 - see
`Project_Vault/Internet Toggle with DNS Lockdown.md`.
